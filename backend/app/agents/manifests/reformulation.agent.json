{
  "name": "reformulation",
  "version": "1.0.0",
  "description": "Refine sub-queries when adequacy is weak.",
  "inputs": {
    "query": { "type": "string", "required": true },
    "sub_queries": { "type": "array", "required": true }
  },
  "outputs": {
    "sub_queries": { "type": "array" },
    "retries_used": { "type": "integer" },
    "trace": { "type": "array" }
  },
  "execution": {
    "type": "python",
    "entrypoint": "app.graph.nodes:reformulation_agent"
  }
}
